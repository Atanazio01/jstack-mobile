import { Platform, StatusBar } from 'react-native';

export const styles = {
  wrapper: {
    flex: 1,
  },
  container: {
    backgroundColor: '#222',
    height: '100%',
  },
  buttonsContainer: {
    gap: 16,
  }
}