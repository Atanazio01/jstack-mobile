import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textArea: {
    borderWidth: 1,
    borderColor: "#888",
    borderRadius: 8,
    color: "#333",
    paddingHorizontal: 16,
    maxHeight: 144,
    minHeight: 72,
  },
  textAreaFocused: {
    borderColor: "#222",
  },
});
