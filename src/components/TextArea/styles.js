import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textArea: {
    borderWidth: 1,
    borderColor: "#888",
    borderRadius: 8,
    height: 72,
    paddingHorizontal: 16,
    color: "#333",
  },
  textAreaFocused: {
    borderColor: "#222",
  },
});
